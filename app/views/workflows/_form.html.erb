<%= form_with(model: workflow, remote: true) do |form| %>
  <%= render 'layouts/error_messages', object: form.object %>
  <div class="row">
  <div class="col-sm-12">
    <div class="actions float-right">
      <%= form.submit 'Submit', class: 'btn btn-link btn-sm' %>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-sm-12">
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, id: :workfow_name %>
    </div>

    <div class="field">
      <%= form.label :description %>
      <%= form.text_area :description, id: :workfow_description , style: "height: 200px;"%>
    </div>

    <div class="field">
      <%= form.label :start_state %>
      <%= form.collection_select(:start_state, @workflow_states, :id, :name, :include_blank => 'None')%>
    </div>
  </div>
</div>
<% end %>
