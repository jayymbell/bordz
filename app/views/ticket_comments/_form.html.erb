<%= form_with(model: ticket_comment, local: true) do |form| %>
  <% if ticket_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_comment.errors.count, "error") %> prohibited this ticket_comment from being saved:</h2>

      <ul>
      <% ticket_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :ticket_comment_comment %>
  </div>

  <div class="field">
    <%= form.label :created_by %>
    <%= form.number_field :created_by, id: :ticket_comment_created_by %>
  </div>

  <div class="field">
    <%= form.label :ticket_id %>
    <%= form.text_field :ticket_id, id: :ticket_comment_ticket_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
