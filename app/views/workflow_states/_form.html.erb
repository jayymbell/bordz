<%= form_with(model: workflow_state, local: true) do |form| %>
  <% if workflow_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(workflow_state.errors.count, "error") %> prohibited this workflow_state from being saved:</h2>

      <ul>
      <% workflow_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :workflow_id %>
    <%= form.text_field :workflow_id, id: :workflow_state_workflow_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :workflow_state_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :workflow_state_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
