<p id="notice"><%= notice %></p>

<h1>Project Roles</h1>

<table>
  <thead>
    <tr>
      <th>Project</th>
      <th>Name</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @project_roles.each do |project_role| %>
      <tr>
        <td><%= project_role.project %></td>
        <td><%= project_role.name %></td>
        <td><%= project_role.description %></td>
        <td><%= link_to 'Show', project_role %></td>
        <td><%= link_to 'Edit', edit_project_role_path(project_role) %></td>
        <td><%= link_to 'Destroy', project_role, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Project Role', new_project_role_path %>
